// export { FrameProvider, useFrame } from './FrameProvider';

// Stub exports to fix imports
export const FrameProvider = ({ children }: { children: React.ReactNode }) => children;
export const useFrame = () => ({});
