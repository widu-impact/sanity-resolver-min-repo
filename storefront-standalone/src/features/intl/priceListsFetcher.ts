// import { getPriceLists as getSdkPriceLists } from '../../lib/sdk/src/server';

import 'server-only';

export function getPriceLists() {


  // return getSdkPriceLists({
  //   clientOptions: {
  //     signal: AbortSignal.timeout(5000),
  //     next: {
  //       tags: ['price-lists'],
  //       revalidate: appConfig.cache.revalidateTime.priceLists,
  //     },
  //   },
  // });

  return null
}
